# DonkLift active backlog

There is no active DonkLift experiment or pending release-automation work.

The one-command release pipeline and its installed package passed real-game
validation on 2026-08-29. Durable build, extraction, packaging, installation,
and safety contracts live in the mod-local source, `README.md`, and `AGENTS.md`.

## Compaction rule

After compaction, reread this file and the mod-local `AGENTS.md` before acting.
Do not replace installed game files while Voyage is running.
